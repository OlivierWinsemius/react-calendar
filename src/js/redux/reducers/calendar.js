const initialState = {
    loading: false,
    errors: null,
    loaded: true,
    weeks: [],
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
